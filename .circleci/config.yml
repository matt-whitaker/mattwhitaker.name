
version: 2
jobs:
  test:
    machine:
      enabled: true
      node:
        version: 12.2.0
    steps:
      - checkout

      - run:
          name: install
          command: npm install

      - run:
          name: build
          command: npm build

      - run:
          name: test
          command: NODE_ENV=test npm test

      - run:
          name: create workspace
          command: mkdir -p workspace

      - run:
          name: copy to workspace
          command: cp -R {lib,node_modules} workspace

      - persist_to_workspace:
          root: workspace
          paths:
            - lib
            - node_modules

  deploy-static:
    machine:
      enabled: true
      node:
        version: 12.2.0
    steps:
      - checkout

      - attach_workspace:
          at: workspace

      - run: cd workspace && cp -R * ..

      - run:
          name: static deploy
          command: npm static-deploy

workflows:
  version: 2
  deploy:
    jobs:
      - test
      - deploy-static:
          requires:
            - test
          filters:
            branches:
              only:
                - develop
                - master
  test:
    jobs:
      - test:
          filters:
            branches:
              ignore:
                - develop
                - master
